# Test Event
An End-to-End testing tool for event based ingest systems.

This tool is used to trigger event based ingest systems like SEASAT.

Example command:
```
tevent --profile asf-sbx --test-bucket asf-cumulus-dev-e2e-tests-discover test --mission seasat --dest-bucket dms-cumulus-dev-seasat-landing
```
