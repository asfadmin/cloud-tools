version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - echo "Installing dependencies..."
      - pip install boto3
  build:
    commands:
      - echo "Preparing SQSes..."
      - python prepare_sqs.py
  post_build:
    commands:
      - echo "SQSes finished."
