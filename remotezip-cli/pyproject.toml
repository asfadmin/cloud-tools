[tool.poetry]
name = "remotezip-cli"
version = "0.1.0"
description = ""
authors = ["Rohan Weeden <reweeden@alaska.edu>"]
readme = "README.md"
packages = [{include = "remotezip_cli"}]

[tool.poetry.scripts]
rz = "remotezip_cli:main"

[tool.poetry.dependencies]
python = ">=3.8,<4"
boto3 = "^1.24.76"
aws-requests-auth = "^0.4.3"
remotezip = "^0.9.4"
humanize = "^4.4.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
